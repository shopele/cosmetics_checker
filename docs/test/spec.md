# テスト仕様書

作成日: 2026-05-19  
バージョン: 1.0

---

## TC-01: rules.js — 化粧品チェック項目の定義確認

**手順**: `js/rules.js` を開き、`RULES.cosmetic.items` の内容を確認する  
**期待結果**: 7項目が定義され、id/name/required/requiredType/article が全て設定されている  
**確認ポイント**: cs_01〜cs_07 が存在し、cs_01〜cs_05 が mandatory、cs_06〜cs_07 が conditional

---

## TC-02: rules.js — 医薬部外品チェック項目の定義確認

**手順**: `js/rules.js` を開き、`RULES.quasi_drug.items` の内容を確認する  
**期待結果**: 10項目が定義され、全て mandatory  
**確認ポイント**: qd_01〜qd_10 が存在、全て required: true

---

## TC-03: 画面初期表示

**手順**: `index.html` をブラウザで開く  
**期待結果**:
- ヘッダーにアプリ名が表示される
- APIキー入力欄が空 or localStorage から読み込まれている
- カテゴリ「化粧品」がデフォルト選択されている
- 「チェック実行」ボタンが非活性
- 履歴エリアに「履歴はありません」が表示される
- フッターに免責事項が表示される

---

## TC-04: APIキー入力によるボタン活性化

**手順**:
1. APIキー欄にテキストを入力する
2. 画像をアップロードする
**期待結果**: 「チェック実行」ボタンが活性化する  
**確認ポイント**: APIキーを入力した時点でもボタン状態が更新される（バグ修正済み）

---

## TC-05: 画像アップロード（ファイル選択）

**手順**: ドロップゾーンをクリック → ファイル選択ダイアログ → 画像ファイルを選択  
**期待結果**: プレビューにサムネイルが表示され、「X枚選択中」が表示される

---

## TC-06: 画像アップロード（ドラッグ&ドロップ）

**手順**: 画像ファイルをドロップゾーンにドラッグ&ドロップ  
**期待結果**: ドラッグオーバー時に枠色が変わり、ドロップ後にプレビューが表示される

---

## TC-07: 画像削除

**手順**: プレビューの「×」ボタンをクリック  
**期待結果**: 該当画像が削除され、残りの画像が正しく表示される

---

## TC-08: チェック実行（正常系）

**手順**: APIキーを入力し、画像をアップロードし、「チェック実行」をクリック  
**期待結果**:
- ボタンが「確認中...」に変わる
- ローディングメッセージが表示される
- 結果が表示される（全体判定バナー + 項目別テーブル）
- 履歴に保存される

---

## TC-09: チェック実行（APIキー未入力）

**手順**: APIキーを入力せずに「チェック実行」をクリック（またはボタンが disabled であることを確認）  
**期待結果**: エラーメッセージ「APIキーを入力してください」が表示される

---

## TC-10: チェック実行（画像未選択）

**手順**: APIキーのみ入力して「チェック実行」をクリック（またはボタンが disabled であることを確認）  
**期待結果**: ボタンが disabled のまま

---

## TC-11: カテゴリ切り替え

**手順**: カテゴリを「化粧品」→「医薬部外品」に切り替える  
**期待結果**: 選択が切り替わる

---

## TC-12: 履歴の表示

**手順**: チェック実行後、履歴エリアを確認する  
**期待結果**: チェック日時・カテゴリ・画像枚数・全体判定が表示される

---

## TC-13: 履歴の詳細表示

**手順**: 履歴行の「詳細」ボタンをクリック  
**期待結果**: 結果エリアにその履歴の項目別結果が表示される

---

## TC-14: CSVエクスポート

**手順**: 履歴が1件以上ある状態で「CSVエクスポート」ボタンをクリック  
**期待結果**: CSVファイルがダウンロードされ、Excelで文字化けなく開ける

---

## TC-15: APIキーのlocalStorage保存

**手順**: APIキーを入力後、ページをリロードする  
**期待結果**: 入力したAPIキーがリロード後も保持されている
